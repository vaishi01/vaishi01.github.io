import type { IconProps, IconComponent } from "../../types";
interface ShellProps extends IconProps {
    type?: "stroke";
}
export declare const Shell: IconComponent<ShellProps>;
export default Shell;
