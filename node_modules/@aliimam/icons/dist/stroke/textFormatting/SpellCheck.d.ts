import type { IconProps, IconComponent } from "../../types";
interface SpellCheckProps extends IconProps {
    type?: "stroke";
}
export declare const SpellCheck: IconComponent<SpellCheckProps>;
export default SpellCheck;
