import type { IconProps, IconComponent } from "../../types";
interface SheetProps extends IconProps {
    type?: "stroke";
}
export declare const Sheet: IconComponent<SheetProps>;
export default Sheet;
