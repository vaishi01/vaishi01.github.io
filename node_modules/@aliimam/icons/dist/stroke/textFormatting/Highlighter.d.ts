import type { IconProps, IconComponent } from "../../types";
interface HighlighterProps extends IconProps {
    type?: "stroke";
}
export declare const Highlighter: IconComponent<HighlighterProps>;
export default Highlighter;
