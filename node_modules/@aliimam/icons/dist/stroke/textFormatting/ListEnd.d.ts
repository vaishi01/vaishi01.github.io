import type { IconProps, IconComponent } from "../../types";
interface ListEndProps extends IconProps {
    type?: "stroke";
}
export declare const ListEnd: IconComponent<ListEndProps>;
export default ListEnd;
