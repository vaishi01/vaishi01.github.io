import type { IconProps, IconComponent } from "../../types";
interface ScrollProps extends IconProps {
    type?: "stroke";
}
export declare const Scroll: IconComponent<ScrollProps>;
export default Scroll;
