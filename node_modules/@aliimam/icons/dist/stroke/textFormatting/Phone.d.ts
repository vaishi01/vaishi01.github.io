import type { IconProps, IconComponent } from "../../types";
interface PhoneProps extends IconProps {
    type?: "stroke";
}
export declare const Phone: IconComponent<PhoneProps>;
export default Phone;
