import type { IconProps, IconComponent } from "../../types";
interface WalletProps extends IconProps {
    type?: "stroke";
}
export declare const Wallet: IconComponent<WalletProps>;
export default Wallet;
