import type { IconProps, IconComponent } from "../../types";
interface InboxProps extends IconProps {
    type?: "stroke";
}
export declare const Inbox: IconComponent<InboxProps>;
export default Inbox;
