import type { IconProps, IconComponent } from "../../types";
interface TrashProps extends IconProps {
    type?: "stroke";
}
export declare const Trash: IconComponent<TrashProps>;
export default Trash;
