import type { IconProps, IconComponent } from "../../types";
interface ArchiveProps extends IconProps {
    type?: "stroke";
}
export declare const Archive: IconComponent<ArchiveProps>;
export default Archive;
