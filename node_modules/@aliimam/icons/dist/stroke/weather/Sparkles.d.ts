import type { IconProps, IconComponent } from "../../types";
interface SparklesProps extends IconProps {
    type?: "stroke";
}
export declare const Sparkles: IconComponent<SparklesProps>;
export default Sparkles;
