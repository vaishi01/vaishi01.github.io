import type { IconProps, IconComponent } from "../../types";
interface ServerCogProps extends IconProps {
    type?: "stroke";
}
export declare const ServerCog: IconComponent<ServerCogProps>;
export default ServerCog;
