import type { IconProps, IconComponent } from "../../types";
interface MonitorProps extends IconProps {
    type?: "stroke";
}
export declare const Monitor: IconComponent<MonitorProps>;
export default Monitor;
