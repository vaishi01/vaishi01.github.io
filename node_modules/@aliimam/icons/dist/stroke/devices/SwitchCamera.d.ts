import type { IconProps, IconComponent } from "../../types";
interface SwitchCameraProps extends IconProps {
    type?: "stroke";
}
export declare const SwitchCamera: IconComponent<SwitchCameraProps>;
export default SwitchCamera;
