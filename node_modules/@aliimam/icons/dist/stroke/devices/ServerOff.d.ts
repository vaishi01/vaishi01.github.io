import type { IconProps, IconComponent } from "../../types";
interface ServerOffProps extends IconProps {
    type?: "stroke";
}
export declare const ServerOff: IconComponent<ServerOffProps>;
export default ServerOff;
