import type { IconProps, IconComponent } from "../../types";
interface MonitorCheckProps extends IconProps {
    type?: "stroke";
}
export declare const MonitorCheck: IconComponent<MonitorCheckProps>;
export default MonitorCheck;
