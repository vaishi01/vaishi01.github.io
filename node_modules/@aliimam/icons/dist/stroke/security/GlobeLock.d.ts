import type { IconProps, IconComponent } from "../../types";
interface GlobeLockProps extends IconProps {
    type?: "stroke";
}
export declare const GlobeLock: IconComponent<GlobeLockProps>;
export default GlobeLock;
