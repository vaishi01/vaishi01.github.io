import type { IconProps, IconComponent } from "../../types";
interface BookKeyProps extends IconProps {
    type?: "stroke";
}
export declare const BookKey: IconComponent<BookKeyProps>;
export default BookKey;
