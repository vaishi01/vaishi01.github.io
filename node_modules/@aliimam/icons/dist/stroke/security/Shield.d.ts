import type { IconProps, IconComponent } from "../../types";
interface ShieldProps extends IconProps {
    type?: "stroke";
}
export declare const Shield: IconComponent<ShieldProps>;
export default Shield;
