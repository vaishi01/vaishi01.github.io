import type { IconProps, IconComponent } from "../../types";
interface FolderKeyProps extends IconProps {
    type?: "stroke";
}
export declare const FolderKey: IconComponent<FolderKeyProps>;
export default FolderKey;
