import type { IconProps, IconComponent } from "../../types";
interface VaultProps extends IconProps {
    type?: "stroke";
}
export declare const Vault: IconComponent<VaultProps>;
export default Vault;
