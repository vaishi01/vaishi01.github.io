import type { IconProps, IconComponent } from "../../types";
interface WrenchProps extends IconProps {
    type?: "stroke";
}
export declare const Wrench: IconComponent<WrenchProps>;
export default Wrench;
