import type { IconProps, IconComponent } from "../../types";
interface ToolCaseProps extends IconProps {
    type?: "stroke";
}
export declare const ToolCase: IconComponent<ToolCaseProps>;
export default ToolCase;
