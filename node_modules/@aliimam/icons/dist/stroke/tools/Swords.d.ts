import type { IconProps, IconComponent } from "../../types";
interface SwordsProps extends IconProps {
    type?: "stroke";
}
export declare const Swords: IconComponent<SwordsProps>;
export default Swords;
