import type { IconProps, IconComponent } from "../../types";
interface PaintBucketProps extends IconProps {
    type?: "stroke";
}
export declare const PaintBucket: IconComponent<PaintBucketProps>;
export default PaintBucket;
