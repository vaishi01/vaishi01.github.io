import type { IconProps, IconComponent } from "../../types";
interface StampProps extends IconProps {
    type?: "stroke";
}
export declare const Stamp: IconComponent<StampProps>;
export default Stamp;
