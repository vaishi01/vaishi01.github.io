import type { IconProps, IconComponent } from "../../types";
interface TangentProps extends IconProps {
    type?: "stroke";
}
export declare const Tangent: IconComponent<TangentProps>;
export default Tangent;
