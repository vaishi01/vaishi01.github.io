import type { IconProps, IconComponent } from "../../types";
interface HexagonProps extends IconProps {
    type?: "stroke";
}
export declare const Hexagon: IconComponent<HexagonProps>;
export default Hexagon;
