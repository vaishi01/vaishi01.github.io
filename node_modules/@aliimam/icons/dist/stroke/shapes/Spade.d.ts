import type { IconProps, IconComponent } from "../../types";
interface SpadeProps extends IconProps {
    type?: "stroke";
}
export declare const Spade: IconComponent<SpadeProps>;
export default Spade;
