import type { IconProps, IconComponent } from "../../types";
interface DiameterProps extends IconProps {
    type?: "stroke";
}
export declare const Diameter: IconComponent<DiameterProps>;
export default Diameter;
