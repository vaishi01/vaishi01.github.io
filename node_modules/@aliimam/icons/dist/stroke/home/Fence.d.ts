import type { IconProps, IconComponent } from "../../types";
interface FenceProps extends IconProps {
    type?: "stroke";
}
export declare const Fence: IconComponent<FenceProps>;
export default Fence;
