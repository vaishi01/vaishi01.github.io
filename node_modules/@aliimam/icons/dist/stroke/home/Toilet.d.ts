import type { IconProps, IconComponent } from "../../types";
interface ToiletProps extends IconProps {
    type?: "stroke";
}
export declare const Toilet: IconComponent<ToiletProps>;
export default Toilet;
