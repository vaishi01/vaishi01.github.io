import type { IconProps, IconComponent } from "../../types";
interface RouterProps extends IconProps {
    type?: "stroke";
}
export declare const Router: IconComponent<RouterProps>;
export default Router;
