import type { IconProps, IconComponent } from "../../types";
interface BugPlayProps extends IconProps {
    type?: "stroke";
}
export declare const BugPlay: IconComponent<BugPlayProps>;
export default BugPlay;
