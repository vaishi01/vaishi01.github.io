import type { IconProps, IconComponent } from "../../types";
interface WavesProps extends IconProps {
    type?: "stroke";
}
export declare const Waves: IconComponent<WavesProps>;
export default Waves;
