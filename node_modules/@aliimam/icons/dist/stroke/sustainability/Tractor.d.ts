import type { IconProps, IconComponent } from "../../types";
interface TractorProps extends IconProps {
    type?: "stroke";
}
export declare const Tractor: IconComponent<TractorProps>;
export default Tractor;
