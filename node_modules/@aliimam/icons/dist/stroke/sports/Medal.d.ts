import type { IconProps, IconComponent } from "../../types";
interface MedalProps extends IconProps {
    type?: "stroke";
}
export declare const Medal: IconComponent<MedalProps>;
export default Medal;
