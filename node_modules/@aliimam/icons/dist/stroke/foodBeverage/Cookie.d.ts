import type { IconProps, IconComponent } from "../../types";
interface CookieProps extends IconProps {
    type?: "stroke";
}
export declare const Cookie: IconComponent<CookieProps>;
export default Cookie;
