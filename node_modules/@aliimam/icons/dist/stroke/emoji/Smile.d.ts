import type { IconProps, IconComponent } from "../../types";
interface SmileProps extends IconProps {
    type?: "stroke";
}
export declare const Smile: IconComponent<SmileProps>;
export default Smile;
