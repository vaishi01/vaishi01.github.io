import type { IconProps, IconComponent } from "../../types";
interface SyringeProps extends IconProps {
    type?: "stroke";
}
export declare const Syringe: IconComponent<SyringeProps>;
export default Syringe;
