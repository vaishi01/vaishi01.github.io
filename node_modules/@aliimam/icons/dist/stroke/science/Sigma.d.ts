import type { IconProps, IconComponent } from "../../types";
interface SigmaProps extends IconProps {
    type?: "stroke";
}
export declare const Sigma: IconComponent<SigmaProps>;
export default Sigma;
