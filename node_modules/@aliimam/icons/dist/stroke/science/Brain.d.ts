import type { IconProps, IconComponent } from "../../types";
interface BrainProps extends IconProps {
    type?: "stroke";
}
export declare const Brain: IconComponent<BrainProps>;
export default Brain;
