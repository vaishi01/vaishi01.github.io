import type { IconProps, IconComponent } from "../../types";
interface TicketsProps extends IconProps {
    type?: "stroke";
}
export declare const Tickets: IconComponent<TicketsProps>;
export default Tickets;
