import type { IconProps, IconComponent } from "../../types";
interface ContainerProps extends IconProps {
    type?: "stroke";
}
export declare const Container: IconComponent<ContainerProps>;
export default Container;
