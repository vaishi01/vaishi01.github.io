import type { IconProps, IconComponent } from "../../types";
interface SearchCodeProps extends IconProps {
    type?: "stroke";
}
export declare const SearchCode: IconComponent<SearchCodeProps>;
export default SearchCode;
