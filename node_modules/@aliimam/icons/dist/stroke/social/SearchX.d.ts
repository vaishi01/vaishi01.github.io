import type { IconProps, IconComponent } from "../../types";
interface SearchXProps extends IconProps {
    type?: "stroke";
}
export declare const SearchX: IconComponent<SearchXProps>;
export default SearchX;
