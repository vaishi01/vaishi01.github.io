import type { IconProps, IconComponent } from "../../types";
interface ContactRoundProps extends IconProps {
    type?: "stroke";
}
export declare const ContactRound: IconComponent<ContactRoundProps>;
export default ContactRound;
