import type { IconProps, IconComponent } from "../../types";
interface ContactProps extends IconProps {
    type?: "stroke";
}
export declare const Contact: IconComponent<ContactProps>;
export default Contact;
