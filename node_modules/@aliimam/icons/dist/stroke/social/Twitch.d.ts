import type { IconProps, IconComponent } from "../../types";
interface TwitchProps extends IconProps {
    type?: "stroke";
}
export declare const Twitch: IconComponent<TwitchProps>;
export default Twitch;
