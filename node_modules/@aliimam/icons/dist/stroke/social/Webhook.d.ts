import type { IconProps, IconComponent } from "../../types";
interface WebhookProps extends IconProps {
    type?: "stroke";
}
export declare const Webhook: IconComponent<WebhookProps>;
export default Webhook;
