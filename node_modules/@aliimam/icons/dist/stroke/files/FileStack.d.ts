import type { IconProps, IconComponent } from "../../types";
interface FileStackProps extends IconProps {
    type?: "stroke";
}
export declare const FileStack: IconComponent<FileStackProps>;
export default FileStack;
