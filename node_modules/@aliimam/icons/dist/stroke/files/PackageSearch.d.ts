import type { IconProps, IconComponent } from "../../types";
interface PackageSearchProps extends IconProps {
    type?: "stroke";
}
export declare const PackageSearch: IconComponent<PackageSearchProps>;
export default PackageSearch;
