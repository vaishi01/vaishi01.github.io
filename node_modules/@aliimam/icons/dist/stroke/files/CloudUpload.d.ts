import type { IconProps, IconComponent } from "../../types";
interface CloudUploadProps extends IconProps {
    type?: "stroke";
}
export declare const CloudUpload: IconComponent<CloudUploadProps>;
export default CloudUpload;
