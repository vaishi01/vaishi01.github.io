import type { IconProps, IconComponent } from "../../types";
interface FolderCodeProps extends IconProps {
    type?: "stroke";
}
export declare const FolderCode: IconComponent<FolderCodeProps>;
export default FolderCode;
