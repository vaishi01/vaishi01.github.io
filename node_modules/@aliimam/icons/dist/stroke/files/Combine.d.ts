import type { IconProps, IconComponent } from "../../types";
interface CombineProps extends IconProps {
    type?: "stroke";
}
export declare const Combine: IconComponent<CombineProps>;
export default Combine;
