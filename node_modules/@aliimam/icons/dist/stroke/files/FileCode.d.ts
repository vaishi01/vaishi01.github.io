import type { IconProps, IconComponent } from "../../types";
interface FileCodeProps extends IconProps {
    type?: "stroke";
}
export declare const FileCode: IconComponent<FileCodeProps>;
export default FileCode;
