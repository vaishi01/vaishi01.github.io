import type { IconProps, IconComponent } from "../../types";
interface PackageProps extends IconProps {
    type?: "stroke";
}
export declare const Package: IconComponent<PackageProps>;
export default Package;
