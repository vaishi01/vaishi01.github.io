import type { IconProps, IconComponent } from "../../types";
interface BlendProps extends IconProps {
    type?: "stroke";
}
export declare const Blend: IconComponent<BlendProps>;
export default Blend;
