import type { IconProps, IconComponent } from "../../types";
interface ImageProps extends IconProps {
    type?: "stroke";
}
export declare const Image: IconComponent<ImageProps>;
export default Image;
