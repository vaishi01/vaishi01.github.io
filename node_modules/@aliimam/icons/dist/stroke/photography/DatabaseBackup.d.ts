import type { IconProps, IconComponent } from "../../types";
interface DatabaseBackupProps extends IconProps {
    type?: "stroke";
}
export declare const DatabaseBackup: IconComponent<DatabaseBackupProps>;
export default DatabaseBackup;
