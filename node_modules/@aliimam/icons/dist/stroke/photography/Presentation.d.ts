import type { IconProps, IconComponent } from "../../types";
interface PresentationProps extends IconProps {
    type?: "stroke";
}
export declare const Presentation: IconComponent<PresentationProps>;
export default Presentation;
