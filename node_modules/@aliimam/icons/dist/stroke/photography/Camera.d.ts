import type { IconProps, IconComponent } from "../../types";
interface CameraProps extends IconProps {
    type?: "stroke";
}
export declare const Camera: IconComponent<CameraProps>;
export default Camera;
