import type { IconProps, IconComponent } from "../../types";
interface ImageDownProps extends IconProps {
    type?: "stroke";
}
export declare const ImageDown: IconComponent<ImageDownProps>;
export default ImageDown;
