import type { IconProps, IconComponent } from "../../types";
interface FrameProps extends IconProps {
    type?: "stroke";
}
export declare const Frame: IconComponent<FrameProps>;
export default Frame;
