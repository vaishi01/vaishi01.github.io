import type { IconProps, IconComponent } from "../../types";
interface BookImageProps extends IconProps {
    type?: "stroke";
}
export declare const BookImage: IconComponent<BookImageProps>;
export default BookImage;
