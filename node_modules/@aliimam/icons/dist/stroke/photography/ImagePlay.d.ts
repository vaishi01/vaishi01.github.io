import type { IconProps, IconComponent } from "../../types";
interface ImagePlayProps extends IconProps {
    type?: "stroke";
}
export declare const ImagePlay: IconComponent<ImagePlayProps>;
export default ImagePlay;
