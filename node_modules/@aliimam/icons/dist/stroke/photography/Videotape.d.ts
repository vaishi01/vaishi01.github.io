import type { IconProps, IconComponent } from "../../types";
interface VideotapeProps extends IconProps {
    type?: "stroke";
}
export declare const Videotape: IconComponent<VideotapeProps>;
export default Videotape;
