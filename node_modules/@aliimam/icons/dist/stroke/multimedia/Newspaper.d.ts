import type { IconProps, IconComponent } from "../../types";
interface NewspaperProps extends IconProps {
    type?: "stroke";
}
export declare const Newspaper: IconComponent<NewspaperProps>;
export default Newspaper;
