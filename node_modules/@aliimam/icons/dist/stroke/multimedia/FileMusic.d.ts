import type { IconProps, IconComponent } from "../../types";
interface FileMusicProps extends IconProps {
    type?: "stroke";
}
export declare const FileMusic: IconComponent<FileMusicProps>;
export default FileMusic;
