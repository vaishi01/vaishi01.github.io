import type { IconProps, IconComponent } from "../../types";
interface VolumeProps extends IconProps {
    type?: "stroke";
}
export declare const Volume: IconComponent<VolumeProps>;
export default Volume;
