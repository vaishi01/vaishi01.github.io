import type { IconProps, IconComponent } from "../../types";
interface SpeakerProps extends IconProps {
    type?: "stroke";
}
export declare const Speaker: IconComponent<SpeakerProps>;
export default Speaker;
