import type { IconProps, IconComponent } from "../../types";
interface DivideProps extends IconProps {
    type?: "stroke";
}
export declare const Divide: IconComponent<DivideProps>;
export default Divide;
