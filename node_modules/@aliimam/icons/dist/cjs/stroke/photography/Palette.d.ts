import type { IconProps, IconComponent } from "../../types";
interface PaletteProps extends IconProps {
    type?: "stroke";
}
export declare const Palette: IconComponent<PaletteProps>;
export default Palette;
