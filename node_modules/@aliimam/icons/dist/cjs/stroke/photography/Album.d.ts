import type { IconProps, IconComponent } from "../../types";
interface AlbumProps extends IconProps {
    type?: "stroke";
}
export declare const Album: IconComponent<AlbumProps>;
export default Album;
