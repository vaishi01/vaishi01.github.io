import type { IconProps, IconComponent } from "../../types";
interface RatioProps extends IconProps {
    type?: "stroke";
}
export declare const Ratio: IconComponent<RatioProps>;
export default Ratio;
