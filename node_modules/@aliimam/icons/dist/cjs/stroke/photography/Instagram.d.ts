import type { IconProps, IconComponent } from "../../types";
interface InstagramProps extends IconProps {
    type?: "stroke";
}
export declare const Instagram: IconComponent<InstagramProps>;
export default Instagram;
