import type { IconProps, IconComponent } from "../../types";
interface VideoProps extends IconProps {
    type?: "stroke";
}
export declare const Video: IconComponent<VideoProps>;
export default Video;
