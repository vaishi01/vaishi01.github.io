import type { IconProps, IconComponent } from "../../types";
interface ImagePlusProps extends IconProps {
    type?: "stroke";
}
export declare const ImagePlus: IconComponent<ImagePlusProps>;
export default ImagePlus;
