import type { IconProps, IconComponent } from "../../types";
interface KanbanProps extends IconProps {
    type?: "stroke";
}
export declare const Kanban: IconComponent<KanbanProps>;
export default Kanban;
