import type { IconProps, IconComponent } from "../../types";
interface SplitProps extends IconProps {
    type?: "stroke";
}
export declare const Split: IconComponent<SplitProps>;
export default Split;
