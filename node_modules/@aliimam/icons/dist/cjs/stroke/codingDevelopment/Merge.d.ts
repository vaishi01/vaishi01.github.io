import type { IconProps, IconComponent } from "../../types";
interface MergeProps extends IconProps {
    type?: "stroke";
}
export declare const Merge: IconComponent<MergeProps>;
export default Merge;
