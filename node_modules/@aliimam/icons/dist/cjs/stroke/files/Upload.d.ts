import type { IconProps, IconComponent } from "../../types";
interface UploadProps extends IconProps {
    type?: "stroke";
}
export declare const Upload: IconComponent<UploadProps>;
export default Upload;
