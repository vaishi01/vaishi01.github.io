import type { IconProps, IconComponent } from "../../types";
interface FolderUpProps extends IconProps {
    type?: "stroke";
}
export declare const FolderUp: IconComponent<FolderUpProps>;
export default FolderUp;
