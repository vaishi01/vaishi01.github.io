import type { IconProps, IconComponent } from "../../types";
interface FolderSyncProps extends IconProps {
    type?: "stroke";
}
export declare const FolderSync: IconComponent<FolderSyncProps>;
export default FolderSync;
