import type { IconProps, IconComponent } from "../../types";
interface FileOutputProps extends IconProps {
    type?: "stroke";
}
export declare const FileOutput: IconComponent<FileOutputProps>;
export default FileOutput;
