import type { IconProps, IconComponent } from "../../types";
interface FileJsonProps extends IconProps {
    type?: "stroke";
}
export declare const FileJson: IconComponent<FileJsonProps>;
export default FileJson;
