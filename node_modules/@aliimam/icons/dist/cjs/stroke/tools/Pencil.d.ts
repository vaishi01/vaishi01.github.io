import type { IconProps, IconComponent } from "../../types";
interface PencilProps extends IconProps {
    type?: "stroke";
}
export declare const Pencil: IconComponent<PencilProps>;
export default Pencil;
