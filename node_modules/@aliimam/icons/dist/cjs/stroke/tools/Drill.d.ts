import type { IconProps, IconComponent } from "../../types";
interface DrillProps extends IconProps {
    type?: "stroke";
}
export declare const Drill: IconComponent<DrillProps>;
export default Drill;
