import type { IconProps, IconComponent } from "../../types";
interface HammerProps extends IconProps {
    type?: "stroke";
}
export declare const Hammer: IconComponent<HammerProps>;
export default Hammer;
