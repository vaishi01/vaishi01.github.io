import type { IconProps, IconComponent } from "../../types";
interface PencilLineProps extends IconProps {
    type?: "stroke";
}
export declare const PencilLine: IconComponent<PencilLineProps>;
export default PencilLine;
