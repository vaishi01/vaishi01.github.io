import type { IconProps, IconComponent } from "../../types";
interface PianoProps extends IconProps {
    type?: "stroke";
}
export declare const Piano: IconComponent<PianoProps>;
export default Piano;
