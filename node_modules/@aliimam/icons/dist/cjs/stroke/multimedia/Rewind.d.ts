import type { IconProps, IconComponent } from "../../types";
interface RewindProps extends IconProps {
    type?: "stroke";
}
export declare const Rewind: IconComponent<RewindProps>;
export default Rewind;
