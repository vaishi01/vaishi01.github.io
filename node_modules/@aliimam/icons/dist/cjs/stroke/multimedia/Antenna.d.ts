import type { IconProps, IconComponent } from "../../types";
interface AntennaProps extends IconProps {
    type?: "stroke";
}
export declare const Antenna: IconComponent<AntennaProps>;
export default Antenna;
