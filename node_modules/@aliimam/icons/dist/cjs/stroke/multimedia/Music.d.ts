import type { IconProps, IconComponent } from "../../types";
interface MusicProps extends IconProps {
    type?: "stroke";
}
export declare const Music: IconComponent<MusicProps>;
export default Music;
