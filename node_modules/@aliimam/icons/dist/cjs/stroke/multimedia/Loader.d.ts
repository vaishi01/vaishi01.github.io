import type { IconProps, IconComponent } from "../../types";
interface LoaderProps extends IconProps {
    type?: "stroke";
}
export declare const Loader: IconComponent<LoaderProps>;
export default Loader;
