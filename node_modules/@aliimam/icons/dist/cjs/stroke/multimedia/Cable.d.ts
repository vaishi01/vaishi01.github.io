import type { IconProps, IconComponent } from "../../types";
interface CableProps extends IconProps {
    type?: "stroke";
}
export declare const Cable: IconComponent<CableProps>;
export default Cable;
