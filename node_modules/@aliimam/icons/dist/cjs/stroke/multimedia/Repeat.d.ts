import type { IconProps, IconComponent } from "../../types";
interface RepeatProps extends IconProps {
    type?: "stroke";
}
export declare const Repeat: IconComponent<RepeatProps>;
export default Repeat;
