import type { IconProps, IconComponent } from "../../types";
interface ReceiptProps extends IconProps {
    type?: "stroke";
}
export declare const Receipt: IconComponent<ReceiptProps>;
export default Receipt;
