import type { IconProps, IconComponent } from "../../types";
interface CompassProps extends IconProps {
    type?: "stroke";
}
export declare const Compass: IconComponent<CompassProps>;
export default Compass;
