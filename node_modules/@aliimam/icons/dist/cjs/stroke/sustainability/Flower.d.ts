import type { IconProps, IconComponent } from "../../types";
interface FlowerProps extends IconProps {
    type?: "stroke";
}
export declare const Flower: IconComponent<FlowerProps>;
export default Flower;
