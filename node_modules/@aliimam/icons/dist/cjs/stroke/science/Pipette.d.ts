import type { IconProps, IconComponent } from "../../types";
interface PipetteProps extends IconProps {
    type?: "stroke";
}
export declare const Pipette: IconComponent<PipetteProps>;
export default Pipette;
