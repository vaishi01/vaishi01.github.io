import type { IconProps, IconComponent } from "../../types";
interface ArchiveRestoreProps extends IconProps {
    type?: "stroke";
}
export declare const ArchiveRestore: IconComponent<ArchiveRestoreProps>;
export default ArchiveRestore;
