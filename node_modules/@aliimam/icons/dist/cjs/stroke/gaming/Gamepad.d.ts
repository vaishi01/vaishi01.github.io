import type { IconProps, IconComponent } from "../../types";
interface GamepadProps extends IconProps {
    type?: "stroke";
}
export declare const Gamepad: IconComponent<GamepadProps>;
export default Gamepad;
