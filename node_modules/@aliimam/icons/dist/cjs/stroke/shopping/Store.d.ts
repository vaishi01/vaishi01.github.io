import type { IconProps, IconComponent } from "../../types";
interface StoreProps extends IconProps {
    type?: "stroke";
}
export declare const Store: IconComponent<StoreProps>;
export default Store;
