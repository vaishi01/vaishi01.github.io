import type { IconProps, IconComponent } from "../../types";
interface PercentProps extends IconProps {
    type?: "stroke";
}
export declare const Percent: IconComponent<PercentProps>;
export default Percent;
