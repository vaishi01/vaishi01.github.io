import type { IconProps, IconComponent } from "../../types";
interface CopyXProps extends IconProps {
    type?: "stroke";
}
export declare const CopyX: IconComponent<CopyXProps>;
export default CopyX;
