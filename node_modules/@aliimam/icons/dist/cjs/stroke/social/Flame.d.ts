import type { IconProps, IconComponent } from "../../types";
interface FlameProps extends IconProps {
    type?: "stroke";
}
export declare const Flame: IconComponent<FlameProps>;
export default Flame;
