import type { IconProps, IconComponent } from "../../types";
interface NotepadTextProps extends IconProps {
    type?: "stroke";
}
export declare const NotepadText: IconComponent<NotepadTextProps>;
export default NotepadText;
