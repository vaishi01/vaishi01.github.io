import type { IconProps, IconComponent } from "../../types";
interface SlackProps extends IconProps {
    type?: "stroke";
}
export declare const Slack: IconComponent<SlackProps>;
export default Slack;
