import type { IconProps, IconComponent } from "../../types";
interface CakeSliceProps extends IconProps {
    type?: "stroke";
}
export declare const CakeSlice: IconComponent<CakeSliceProps>;
export default CakeSlice;
