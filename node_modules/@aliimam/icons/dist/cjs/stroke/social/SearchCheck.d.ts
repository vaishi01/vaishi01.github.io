import type { IconProps, IconComponent } from "../../types";
interface SearchCheckProps extends IconProps {
    type?: "stroke";
}
export declare const SearchCheck: IconComponent<SearchCheckProps>;
export default SearchCheck;
