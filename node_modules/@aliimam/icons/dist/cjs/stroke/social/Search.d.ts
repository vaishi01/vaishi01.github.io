import type { IconProps, IconComponent } from "../../types";
interface SearchProps extends IconProps {
    type?: "stroke";
}
export declare const Search: IconComponent<SearchProps>;
export default Search;
