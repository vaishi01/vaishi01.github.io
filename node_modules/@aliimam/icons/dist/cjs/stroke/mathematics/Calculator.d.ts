import type { IconProps, IconComponent } from "../../types";
interface CalculatorProps extends IconProps {
    type?: "stroke";
}
export declare const Calculator: IconComponent<CalculatorProps>;
export default Calculator;
