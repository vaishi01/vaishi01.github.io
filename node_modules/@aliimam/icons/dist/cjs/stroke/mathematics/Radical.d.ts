import type { IconProps, IconComponent } from "../../types";
interface RadicalProps extends IconProps {
    type?: "stroke";
}
export declare const Radical: IconComponent<RadicalProps>;
export default Radical;
