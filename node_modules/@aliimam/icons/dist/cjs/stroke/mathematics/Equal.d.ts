import type { IconProps, IconComponent } from "../../types";
interface EqualProps extends IconProps {
    type?: "stroke";
}
export declare const Equal: IconComponent<EqualProps>;
export default Equal;
