import type { IconProps, IconComponent } from "../../types";
interface OrigamiProps extends IconProps {
    type?: "stroke";
}
export declare const Origami: IconComponent<OrigamiProps>;
export default Origami;
