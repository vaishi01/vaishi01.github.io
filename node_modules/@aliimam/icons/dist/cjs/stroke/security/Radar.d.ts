import type { IconProps, IconComponent } from "../../types";
interface RadarProps extends IconProps {
    type?: "stroke";
}
export declare const Radar: IconComponent<RadarProps>;
export default Radar;
