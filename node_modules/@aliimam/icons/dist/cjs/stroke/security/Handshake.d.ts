import type { IconProps, IconComponent } from "../../types";
interface HandshakeProps extends IconProps {
    type?: "stroke";
}
export declare const Handshake: IconComponent<HandshakeProps>;
export default Handshake;
