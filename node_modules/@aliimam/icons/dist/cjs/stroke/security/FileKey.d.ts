import type { IconProps, IconComponent } from "../../types";
interface FileKeyProps extends IconProps {
    type?: "stroke";
}
export declare const FileKey: IconComponent<FileKeyProps>;
export default FileKey;
