import type { IconProps, IconComponent } from "../../types";
interface DumbbellProps extends IconProps {
    type?: "stroke";
}
export declare const Dumbbell: IconComponent<DumbbellProps>;
export default Dumbbell;
