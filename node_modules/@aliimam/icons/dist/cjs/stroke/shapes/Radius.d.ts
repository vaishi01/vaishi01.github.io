import type { IconProps, IconComponent } from "../../types";
interface RadiusProps extends IconProps {
    type?: "stroke";
}
export declare const Radius: IconComponent<RadiusProps>;
export default Radius;
