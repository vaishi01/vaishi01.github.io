import type { IconProps, IconComponent } from "../../types";
interface BlocksProps extends IconProps {
    type?: "stroke";
}
export declare const Blocks: IconComponent<BlocksProps>;
export default Blocks;
