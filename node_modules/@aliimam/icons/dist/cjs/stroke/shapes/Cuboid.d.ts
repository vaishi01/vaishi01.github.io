import type { IconProps, IconComponent } from "../../types";
interface CuboidProps extends IconProps {
    type?: "stroke";
}
export declare const Cuboid: IconComponent<CuboidProps>;
export default Cuboid;
