import type { IconProps, IconComponent } from "../../types";
interface SquareProps extends IconProps {
    type?: "stroke";
}
export declare const Square: IconComponent<SquareProps>;
export default Square;
