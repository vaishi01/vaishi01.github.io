import type { IconProps, IconComponent } from "../../types";
interface UngroupProps extends IconProps {
    type?: "stroke";
}
export declare const Ungroup: IconComponent<UngroupProps>;
export default Ungroup;
