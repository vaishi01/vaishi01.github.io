import type { IconProps, IconComponent } from "../../types";
interface TorusProps extends IconProps {
    type?: "stroke";
}
export declare const Torus: IconComponent<TorusProps>;
export default Torus;
