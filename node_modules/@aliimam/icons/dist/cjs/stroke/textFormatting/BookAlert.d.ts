import type { IconProps, IconComponent } from "../../types";
interface BookAlertProps extends IconProps {
    type?: "stroke";
}
export declare const BookAlert: IconComponent<BookAlertProps>;
export default BookAlert;
