import type { IconProps, IconComponent } from "../../types";
interface KeyboardProps extends IconProps {
    type?: "stroke";
}
export declare const Keyboard: IconComponent<KeyboardProps>;
export default Keyboard;
