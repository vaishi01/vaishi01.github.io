import type { IconProps, IconComponent } from "../../types";
interface ExpandProps extends IconProps {
    type?: "stroke";
}
export declare const Expand: IconComponent<ExpandProps>;
export default Expand;
