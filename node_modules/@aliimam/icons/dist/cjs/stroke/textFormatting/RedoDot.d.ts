import type { IconProps, IconComponent } from "../../types";
interface RedoDotProps extends IconProps {
    type?: "stroke";
}
export declare const RedoDot: IconComponent<RedoDotProps>;
export default RedoDot;
