import type { IconProps, IconComponent } from "../../types";
interface FileTextProps extends IconProps {
    type?: "stroke";
}
export declare const FileText: IconComponent<FileTextProps>;
export default FileText;
