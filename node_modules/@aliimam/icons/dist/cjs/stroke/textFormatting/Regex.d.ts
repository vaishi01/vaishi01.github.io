import type { IconProps, IconComponent } from "../../types";
interface RegexProps extends IconProps {
    type?: "stroke";
}
export declare const Regex: IconComponent<RegexProps>;
export default Regex;
