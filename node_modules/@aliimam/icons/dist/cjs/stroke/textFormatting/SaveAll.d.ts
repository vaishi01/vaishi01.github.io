import type { IconProps, IconComponent } from "../../types";
interface SaveAllProps extends IconProps {
    type?: "stroke";
}
export declare const SaveAll: IconComponent<SaveAllProps>;
export default SaveAll;
