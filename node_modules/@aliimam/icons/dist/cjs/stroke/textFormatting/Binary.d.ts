import type { IconProps, IconComponent } from "../../types";
interface BinaryProps extends IconProps {
    type?: "stroke";
}
export declare const Binary: IconComponent<BinaryProps>;
export default Binary;
