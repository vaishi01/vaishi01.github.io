import type { IconProps, IconComponent } from "../../types";
interface BookMarkedProps extends IconProps {
    type?: "stroke";
}
export declare const BookMarked: IconComponent<BookMarkedProps>;
export default BookMarked;
