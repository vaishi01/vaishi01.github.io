import type { IconProps, IconComponent } from "../../types";
interface SchoolProps extends IconProps {
    type?: "stroke";
}
export declare const School: IconComponent<SchoolProps>;
export default School;
