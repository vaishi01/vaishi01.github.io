import type { IconProps, IconComponent } from "../../types";
interface LandmarkProps extends IconProps {
    type?: "stroke";
}
export declare const Landmark: IconComponent<LandmarkProps>;
export default Landmark;
