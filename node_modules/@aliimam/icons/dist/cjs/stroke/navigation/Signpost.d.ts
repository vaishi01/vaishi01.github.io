import type { IconProps, IconComponent } from "../../types";
interface SignpostProps extends IconProps {
    type?: "stroke";
}
export declare const Signpost: IconComponent<SignpostProps>;
export default Signpost;
