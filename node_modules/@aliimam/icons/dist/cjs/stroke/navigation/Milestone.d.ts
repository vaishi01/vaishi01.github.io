import type { IconProps, IconComponent } from "../../types";
interface MilestoneProps extends IconProps {
    type?: "stroke";
}
export declare const Milestone: IconComponent<MilestoneProps>;
export default Milestone;
