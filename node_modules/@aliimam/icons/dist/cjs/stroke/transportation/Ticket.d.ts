import type { IconProps, IconComponent } from "../../types";
interface TicketProps extends IconProps {
    type?: "stroke";
}
export declare const Ticket: IconComponent<TicketProps>;
export default Ticket;
