import type { IconProps, IconComponent } from "../../types";
interface PlaneProps extends IconProps {
    type?: "stroke";
}
export declare const Plane: IconComponent<PlaneProps>;
export default Plane;
