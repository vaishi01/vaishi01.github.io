import type { IconProps, IconComponent } from "../../types";
interface CigaretteProps extends IconProps {
    type?: "stroke";
}
export declare const Cigarette: IconComponent<CigaretteProps>;
export default Cigarette;
