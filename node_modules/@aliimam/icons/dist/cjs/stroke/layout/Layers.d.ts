import type { IconProps, IconComponent } from "../../types";
interface LayersProps extends IconProps {
    type?: "stroke";
}
export declare const Layers: IconComponent<LayersProps>;
export default Layers;
