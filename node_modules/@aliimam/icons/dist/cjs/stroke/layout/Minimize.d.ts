import type { IconProps, IconComponent } from "../../types";
interface MinimizeProps extends IconProps {
    type?: "stroke";
}
export declare const Minimize: IconComponent<MinimizeProps>;
export default Minimize;
