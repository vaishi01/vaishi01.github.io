import type { IconProps, IconComponent } from "../../types";
interface AppWindowProps extends IconProps {
    type?: "stroke";
}
export declare const AppWindow: IconComponent<AppWindowProps>;
export default AppWindow;
