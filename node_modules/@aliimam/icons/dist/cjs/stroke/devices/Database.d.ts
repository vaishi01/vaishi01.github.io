import type { IconProps, IconComponent } from "../../types";
interface DatabaseProps extends IconProps {
    type?: "stroke";
}
export declare const Database: IconComponent<DatabaseProps>;
export default Database;
