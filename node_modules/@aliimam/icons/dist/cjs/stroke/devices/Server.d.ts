import type { IconProps, IconComponent } from "../../types";
interface ServerProps extends IconProps {
    type?: "stroke";
}
export declare const Server: IconComponent<ServerProps>;
export default Server;
