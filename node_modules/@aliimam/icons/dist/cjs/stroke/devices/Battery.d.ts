import type { IconProps, IconComponent } from "../../types";
interface BatteryProps extends IconProps {
    type?: "stroke";
}
export declare const Battery: IconComponent<BatteryProps>;
export default Battery;
