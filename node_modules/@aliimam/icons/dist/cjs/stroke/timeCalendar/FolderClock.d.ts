import type { IconProps, IconComponent } from "../../types";
interface FolderClockProps extends IconProps {
    type?: "stroke";
}
export declare const FolderClock: IconComponent<FolderClockProps>;
export default FolderClock;
